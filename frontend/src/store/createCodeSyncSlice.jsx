// Kept as an empty slice so the shared lesson store has the same composition
// without carrying the private coding-workspace and checkpoint feature set.
export const createCodeSyncSlice = () => ({});
